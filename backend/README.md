# Bowen Backend (NestJS)

## Run
```bash
cp .env.example .env
npm install
npm run start:dev
```

API base URL: `http://localhost:3000/api`

## Default seeded admin
- `admin@bowen.local`
- `Admin123!@#`

Change credentials in `.env` for real usage.
